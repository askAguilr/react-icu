const emptyCode = `const MyComponent = props => {
//TODO: Write render computations here, such as:
//useEffect(...);
//const {userName} = props;
//const myVar = 'Hello ' + userName;

return (MAGICALLY_GENERATED_JSX); 
}
`

export const emptyTemplate ={
    code:emptyCode,
    html:'',
    css:'',
}

